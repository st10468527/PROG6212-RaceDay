# stage all changes 
git add. 
$ git commit -m "created a database called RaceDayDB"
$ git commit -m "started to create tables"
$ git commit -m "created a table called Organisers"
$ git commit -m "added attributes in the Organisers table"
$ git commit -m "created a table - participants"
$ git commit -m "added attributes to the table (participants)"
$ git commit - m "ran into some syntax error on lie 14 and 23 but managed to fix it "
$ git commit -m "added an events table"
$ git commit -m "added attributes to the table"
